package com.dajie.message.mcp.passport;

public class PassportRedisCache {
	  //private static final Log logger = LogFactory.getLog(PassportRedisCache.class);

	    //public static RedisClient redisClient = null;

//	    private static String prefix = "mcp_mid_t2_pspt_";

	   // static {
	      //  try {
//	            ConfigurationService cs = ConfigurationService.getInstance();
//	            String zkIp = cs.getzkConfigProperty("zk_host");
//	            String clusterName = cs.getcacheConfigProperty("mcp_cluster");
//	            redisClient = new RedisClusterPoolClient(clusterName, zkIp);
	          //  redisClient = new RedisClient("mid_passport");
//	            logger.info("PassportRedisCache: zk:" + zkIp + " " + clusterName);
	       // } catch (Exception e) {
	           // logger.error("PassportServiceImpl RedisClusterPoolClient", e);
	       // }
	   // }
}
